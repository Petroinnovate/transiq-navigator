// @ts-nocheck
// Stub: this project uses Vite + react-router-dom (no TanStack Start runtime).
// Auth is enforced via Supabase RLS using the browser client's session.
export const requireSupabaseAuth = null as unknown as never;
